@Library('shared-library') _
pipeline{
    agent any
    stages{
        stage('eho'){
            steps{
                helloWorld(dayOfWeek:"Monday", name:"Krishan")
            }
        }
    }
}